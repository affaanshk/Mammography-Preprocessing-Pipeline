# Model Weights

Model weights are intentionally not distributed as part of this repository. 

To use the `MammographyPreprocessingPipeline` engine in inference mode, ensure you supply a valid path to your locally trained segmentation checkpoint (`.pth`) file upon class instantiation.